## Why

#138 formalizes the architecture reset from the current same-Issue formal-correction Explore `issuecomment-5482546619` on `main@f8f2c49d255997889dcdd406cabd504f03367f07`. That result revalidates and preserves the earlier architecture evidence from `issuecomment-5474475020` while incorporating Human architecture clarifications `issuecomment-5475109024` and `issuecomment-5477274582`. The #168 Explore `ACTION_RESULT(PROPOSAL_READY)` `issuecomment-5442745071` is the bounded transport source/evidence consumed by this formalization; the later Human clarification `issuecomment-5475109024` explicitly makes that transport outcome part of the #138 control-plane boundary and first delivery stage, so the persisted #168 Explore is not a separate implementation owner whose result must be reimplemented in a competing Change. The root cause is duplicated machine workflow ownership across routing labels, prose topology, history parsing, transport and wake orchestration; the latest correction also resolves two execution-boundary defects: exact revision `R` can exist without a runnable exact-`R` validator, and repository effect authority is currently conflated with the `GITHUB_TOKEN` mutation identity used by the Actions application.

The second defect is bounded by the preserved Human environment constraint `issuecomment-5303723685`: inability of GitHub Actions to create the required PR presentation is an environment/deployment constraint, not a permission gap to solve by enabling `Allow GitHub Actions to create and approve pull requests`. Executor `SPEC_BLOCKER` `issuecomment-5504609193` confirms that the current #138 implementation reintroduced that conflict when application run `33589668328` reached `pull-request-create` and received HTTP 403. This correction preserves the Human outcome and changes the mechanism, not the requirement.

Required outcome: ChatGPT Scheduled Tasks are the only normal model wake; GitHub Actions run deterministic code only; no OpenAI/Responses/other model API worker. Repository code/kernel owns mechanical observation, dispatch, typed-result validation, legal effect derivation, carrier eligibility, fresh authorization, stale/replay classification, and postcondition acceptance. A GitHub mutation carrier only executes an exact already-authorized effect and does not become workflow authority. Lead/Reviewer/Executor retain semantic judgment. Preserve Change `simplify-scheduled-agent-control-plane`; reuse an already-open legal PR presentation when it can represent the exact accepted head, and do not create a replacement merely because the application `GITHUB_TOKEN` cannot create PRs.

## What Changes

- Canonical routing becomes one `action:*`; Role is derived. `Change:` remains formal identity; Issue open/closed remains lifecycle state. Results/reviews/Human decisions are evidence; HANDOFF/transport/history are audit.
- Replace generic `merge-pr` Action with `merge-implementation-pr` and `merge-archive-pr`.
- One executable kernel owns Action vocabulary, Action->Role, finite result/transition rules, WIP/FIFO/debt dispatch, effect capability, fresh authorization, stale/replay and postconditions. `agents/workflow.md` becomes generated/mechanically verified presentation, not a production-parsed DAG.
- Workers return bounded typed results plus evidence. Repository application derives only kernel-legal effects, binds each consequential effect to an exact target/precondition/revision and a legal carrier class, then accepts success only after fresh repository-owned postcondition observation.
- **Effect authority and mutation carrier are separate.** `GITHUB_TOKEN`/Actions may execute deterministic effects only where its identity and event semantics satisfy the governed lifecycle. PR create/presentation/head/ready/merge effects that require normal GitHub event propagation or are forbidden to Actions use an event-capable Scheduled-Agent connector/GitHub-App carrier. The carrier receives an exact repository-authorized mutation plan; it MUST NOT select Issue/Action/effect, weaken preconditions, infer retry, or make its own success authoritative. Repository code fresh-verifies the exact resulting object/head/state before any routing, gate, lifecycle, or successor consequence.
- The target solution MUST NOT enable `Allow GitHub Actions to create and approve pull requests`. Preserve the already-approved Archive boundary from `issuecomment-5303723685`: deterministic archive mutation/validation/commit/archive-branch push ends repository automation success; final Archive PR presentation is a normal Scheduled Lead lifecycle effect through the legal PR carrier, followed by independent review and Executor merge.
- One wake executes exactly one authorized mapped Action, work-conserving inside it, then exits after its governed application/carrier/postcondition boundary. A successor always waits for later fresh dispatch.
- Adopt #168 `issuecomment-5442745071` as the transport source and selected buildable direction inside this active Change, as explicitly approved by Human clarification `issuecomment-5475109024`: runtime Issue comments are request/trigger/audit only. Normal dispatch/application/validation results belong to one exact Actions run and are consumed from its run-scoped surface. Exact request->run->result correlation is required; missing/multiple/failed/cancelled/malformed/expired evidence fails closed with no Issue-response fallback. Coordination-Issue semantic comments remain evidence. The existing #168 coordination Issue remains provenance for that already-completed Explore result rather than a competing implementation owner. Stage 5 SHALL consume one reviewed machine-readable absorbed-source retirement entry that names exact source Issue #168, its expected `open + Change: unset + agent:lead + action:explore-change` source state, absorbing Change `simplify-scheduled-agent-control-plane`, and the exact durable source/assignment references. Repository application fresh-verifies that typed entry and current state, closes the source Issue, removes only workflow `agent:*`/`action:*` labels, preserves its body/comments/results and unrelated labels, and accepts only the fresh-observed `closed + no workflow routing` postcondition. The entry is bounded cutover input and audit traceability, not a normal dispatcher selector or a #138-specific runtime exception; missing, duplicate, stale, ambiguous, contradictory, or newer-unresolved-Human evidence fails closed without suppressing work by prose/history/model inference.
- Exact-revision OpenSpec readiness remains an application-owned deterministic resource derived from the governed readiness/effect/artifact requirement rather than a hard-coded source Role/Action whitelist. Repository application must support validating exact target revision `R` whether `R` was just produced by an authorized content mutation or is already the current Change/branch/PR head with no artifact rewrite needed; it MUST NOT rewrite or dummy-touch an already-correct artifact merely to trigger validation. Accepted evidence must prove target `R`, validator checkout `HEAD == R`, qualified pinned OpenSpec compatibility, and strict validation PASS. Stale CI, `run.head_sha` without checkout proof, manual approval workarounds, an ungoverned direct connector mutation, or another model wake cannot satisfy the gate.
- Before first independent `review-openspec` acceptance, a material formalization defect in the Explore source/evidence/feasibility premise may return the same Issue to Explore after provisional non-`unset` activation while preserving Change, PR/artifact history, evidence and WIP. After acceptance, use resolve-question plus independent review.
- Preserve WIP=1, Human authority, completeness/provenance, role separation, exact-revision gates, semantic evidence, stale/concurrency fail-closed behavior and deterministic archive automation.
- Mandatory N-1 Stage 1 is now ordered **1A exact-revision application resource -> 1B transport de-mailbox/daily check-in -> 1C authority/carrier split for identity-sensitive PR lifecycle effects**; after all three are deployed, continue with (2) kernel shadow, (3) typed application, (4) wake simplification, (5) Action-only/explicit-merge cutover plus typed retirement of explicitly absorbed pre-activation sources, and (6) deletion/context reduction. Stage 1A remains the special semantic-review prerequisite because a materially revised Propose/Resolve must validate the exact handoff revision. Stage 1B and 1C are mandatory before Stage 2 but do not independently add a semantic OpenSpec review gate once exact Stage-1A validation is valid. Stage 1C must include the bootstrap/recovery case so existing legal PR presentation can be reused when possible and the system never requires Actions PR-create permission merely to deploy the carrier split. Each stage states outcomes advanced/still incomplete, N-1 prerequisite, rollback/cutover boundary and continuation; split unsafe stages rather than weaken the parent.
- Completion deletes/disables superseded Role routing, response mailbox/history coupling, Markdown topology/effect parsing, same-wake continuation/wake barriers, obsolete compatibility hot paths, legacy Responses/model-worker host code, Actions-owned identity-sensitive PR mutations, and redundant machine-control prose/tests. No permanent dual control plane.

## Capabilities

### Modified Capabilities

- `scheduled-agent-workflow`: canonical state/routing, explicit merge Actions, run-scoped transport, self-hosted exact-revision validation, authority/carrier separation for PR lifecycle effects, typed application, one-action-per-wake, bounded pre-acceptance Explore correction, staged cutover/deletion.
- `repository-governance`: executable kernel becomes the single machine-decidable workflow-semantics/effect-authorization owner; replaceable mutation carriers remain actuators and Human-readable governance/Skills retain semantic ownership.

## Propose readiness evidence

Merged PR #185 is deployed on N-1 at `main@013510b12c5d3cde869308a319a1e2fb12cdfa60`; its replacement PR preserved exact head `bbc2928839bc0cfac7a1fb383b6ece9e9e1bb291`, and PR #185's exact-head OpenSpec Validate (`33472126991`) and Python Quality (`33472127041`) both passed before merge. That proved the first validation bootstrap buildable and deployed. Current `main@e23a7af19815ef35fb0b6c3d13d8fee446059d40` plus the clean correction branch now contain the generalized Stage 1A implementation; exact current review-readiness still depends on validation of the final materially revised handoff revision, not this historical evidence.

The carrier defect is independently evidenced by Human environment constraint `issuecomment-5303723685` and current Executor `SPEC_BLOCKER` `issuecomment-5504609193`: Actions application reached an exact `pull-request-create` effect and failed HTTP 403. This MUST NOT be dispositioned as “enable Actions PR permission and retry.” PR #178 remains open/mergeable at head `5007e6a1ea35077931f6b24802a61864e658dd31`; the clean correction branch is `agent/simplify-scheduled-agent-control-plane-clean` from current `main` and was fresh-observed at `1eaba42b534ccbb46cbdb3e99193fb13b0730392` before this correction. Immediate recovery should prefer converging/reusing an existing legal presentation when exact-head semantics can be preserved rather than manufacturing a replacement PR solely to bypass the 403.

The #168 transport direction is already decision-complete source evidence through `issuecomment-5442745071` and Human clarification `issuecomment-5475109024`; its production deployment is Stage 1B. Stage 1B and Stage 1C block Stage 2, not the current semantic OpenSpec review gate after the final exact handoff revision satisfies Stage 1A.

## Skill maintenance traceability

Current causal source for every entry: #138 formal-correction Explore `issuecomment-5482546619`; preserved upstream architecture evidence `issuecomment-5474475020`; #168 transport Explore result `issuecomment-5442745071`; Human clarifications `issuecomment-5475109024` and `issuecomment-5477274582`; preserved Human environment constraint `issuecomment-5303723685`; Executor specification blocker `issuecomment-5504609193`; Change `simplify-scheduled-agent-control-plane`.

| Skill | Class | Before / preserved responsibility | After / rationale / supersession |
| --- | --- | --- | --- |
| `openspec-explore` | Modified | Research/evidence, feasibility/materiality, Human boundary, Explore dispositions; also routing/continuation mechanics. | Preserve semantic Explore work; kernel/application supersede routing, cuccessor and wake mechanics. |
| `openspec-change` | Modified | Proposal/spec/design/tasks, causal Explore verification, scope/contract, traceability/readiness; also application/routing mechanics. | Preserve Lead specification authority and exact-revision readiness; add approved pre-acceptance Explore correction; deterministic application/validation self-host exact-revision resources while the kernel owns effect authorization and the legal PR carrier executes only exact authorized presentation effects. |
| `openspec-review` | Modified | Independent source-chain + reverse/forward semantic review, Skill/config checks and validation; also handoff mechanics. | Preserve independent PASS/FINDINGS and exact-target validation verification; kernel owns structural Action/successor/effect authorization. |
| `implementation` | Modified | Approved Apply context, RED-GREEN-REFACTOR-VERIFY, checkpoints, spec blockers; current continuation semantics. | Preserve implementation/testing/checkpoints inside `implement-change`; application/carrier postcondition boundary ends the wake and supersedes successor execution. |
| `implementation-review` | Modified | Exact-head conformance/tests/gates/Skill trace and finding class; PASS -> generic merge. | Preserve exact-head independent gate; PASS feeds `merge-implementation-pr`; generic merge Action is superseded here. |
| `archive-review` | Modified | Exact Archive head/canonical/preparation/cleanup-retention gate; PASS -> generic merge. | Preserve Archive gate; PASS feeds `merge-archive-pr`; generic merge phase inference is superseded here. |
| `merge-pr` | Modified | Generic Action infers phase while enforcing exact PASS/head/check/linkage/native-closing/preparation/cleanup/Human safety. | Keep one shared merge procedure for two explicit merge Actions and all applicable safety gates; identity-sensitive merge execution uses the legal carrier while kernel/application retains exact authorization/postcondition authority. |
| `lifecycle-finalize` | Modified | Post-merge lifecycle, archive automation/PR preparation, trackers/cleanup, terminal close; also finite routing details. | Preserve Lead lifecycle/archive/terminal judgment and the validated-archive-branch boundary; final Archive PR presentation uses the legal carrier; kernel/explicit merge Actions supersede finite routing/continuation mechanics. |

No Skill is removed/replaced and no new Skill is added. `skill-creator` remains guidance only and is not materially modified. The generic `merge-pr` **Action** is superseded; the `merge-pr` Skill remains the shared merge procedure.

## Impact

Affected surfaces: shared governance, `agents/workflow.md`, the eight Skills above, message references, both capability specs, and dispatcher/runtime/worker/effect/transport/application/validation/carrier code plus production-boundary tests. Final production must have fewer independent control-state representations and executable decision paths than current `main`, while mutation identity becomes replaceable without changing workflow semantics or effect authority.

Refs #58
Refs #138
Refs #168
