## Why

#138 formalizes the architecture reset from the current same-Issue formal-correction Explore `issuecomment-5482546619` on `main@f8f2c49d255997889dcdd406cabd504f03367f07`. That result revalidates and preserves the earlier architecture evidence from `issuecomment-5474475020` while incorporating Human architecture clarifications `issuecomment-5475109024` and `issuecomment-5477274582`. The #168 Explore `ACTION_RESULT(PROPOSAL_READY)` `issuecomment-5442745071` is the bounded transport source/evidence consumed by this formalization; the later Human clarification `issuecomment-5475109024` explicitly makes that transport outcome part of the #138 control-plane boundary and first delivery stage, so the persisted #168 Explore is not a separate implementation owner whose result must be reimplemented in a competing Change. The root cause is duplicated machine workflow ownership across routing labels, prose topology, history parsing, transport and wake orchestration; the latest correction also resolves the bounded feasibility question created by the execution-boundary deadlock where repository-owned OpenSpec mutation can create revision `R` without a runnable exact-`R` validator.

Required outcome: ChatGPT Scheduled Tasks are the only normal model wake; GitHub Actions run deterministic code only; no OpenAI/Responses/other model API worker. Repository code owns mechanical observation/dispatch/typed-result validation/effects/fresh reauthorization/postconditions and exact-revision deterministic validation resources; Lead/Reviewer/Executor retain semantic judgment. Reuse Change `simplify-scheduled-agent-control-plane` and PR #178.

## What Changes

- Canonical routing becomes one `action:*`; Role is derived. `Change:` remains formal identity; Issue open/closed remains lifecycle state. Results/reviews/Human decisions are evidence; HANDOFF/transport/history are audit.
- Replace generic `merge-pr` Action with `merge-implementation-pr` and `merge-archive-pr`.
- One executable kernel owns Action vocabulary, Action->Role, finite result/transition rules, WIP/FIFO/debt dispatch, effect capability, fresh authorization, stale/replay and postconditions. `agents/workflow.md` becomes generated/mechanically verified presentation, not a production-parsed DAG.
- Workers return bounded typed results plus evidence; application derives only kernel-legal effects and fresh-verifies source/effect predicates.
- Within one machine-authorized Action, execution planning uses one bounded vertical slice with an independently verifiable outcome: `Reconstruct -> RED exact gap -> GREEN legal correction -> VERIFY exact postcondition/revision/gate -> durable checkpoint`. Split before execution when a slice cannot reasonably reach VERIFY in one normal invocation. One file mutation, API call, Actions run, or other intermediate mechanical success is not by itself a completed slice.
- One wake executes exactly one authorized mapped Action, work-conserving inside it, then exits after application. A successor always waits for later fresh dispatch.
- Adopt #168 `issuecomment-5442745071` as the transport source and selected buildable direction inside this active Change, as explicitly approved by Human clarification `issuecomment-5475109024`: runtime Issue comments are request/trigger/audit only. Normal dispatch/application/validation results belong to one exact Actions run and are consumed from its run-scoped surface. Exact request->run->result correlation is required; missing/multiple/failed/cancelled/malformed/expired evidence fails closed with no Issue-response fallback. Coordination-Issue semantic comments remain evidence. The existing #168 coordination Issue remains provenance for that already-completed Explore result rather than a competing implementation owner. Stage 5 SHALL consume one reviewed machine-readable absorbed-source retirement entry that names exact source Issue #168, its expected `open + Change: unset + agent:lead + action:explore-change` source state, absorbing Change `simplify-scheduled-agent-control-plane`, and the exact durable source/assignment references. Repository application fresh-verifies that typed entry and current state, closes the source Issue, removes only workflow `agent:*`/`action:*` labels, preserves its body/comments/results and unrelated labels, and accepts only the fresh-observed `closed + no workflow routing` postcondition. The entry is bounded cutover input and audit traceability, not a normal dispatcher selector or a #138-specific runtime exception; missing, duplicate, stale, ambiguous, contradictory, or newer-unresolved-Human evidence fails closed without suppressing work by prose/history/model inference.
- Exact-revision OpenSpec readiness becomes an application-owned deterministic resource derived from the governed readiness/effect/artifact requirement rather than a hard-coded source Role/Action whitelist. Repository application must support validating the exact current target revision `R` whether `R` was just produced by the current mutation or is already the current Change/PR head with no artifact rewrite needed; it MUST NOT rewrite or dummy-touch an already-correct artifact merely to trigger validation. Accepted evidence must prove target revision `R`, validator checkout `HEAD == R`, qualified pinned OpenSpec compatibility, and strict validation PASS. Stale CI, `run.head_sha` without checkout proof, manual approval workarounds, direct connector bypass, or another model wake cannot satisfy the gate.
- OpenSpec work-product ingress is a distinct content-addressed application boundary, not control/request transport, run-scoped result transport, effect authorization, or the identity-sensitive PR carrier. The semantic worker may create only unreferenced Git blobs as untrusted ingress and carries only exact PR/branch/base plus path/blob-SHA/current-SHA manifest identity. Repository application fresh-reauthorizes the source, validates those identities, constructs one exact multi-file tree and single commit revision `R`, advances only the exact current branch without force, fresh-observes ref/PR/commit/file postconditions, and feeds `R` to exact-revision validation. Canonical cross-role HANDOFF remains application-owned after source result/routing and target routing are observed. The bounded M0 bootstrap may provide N-1 self-hosting for the correction but does not complete formal Stage 1B or #138; production live E2E remains required.
- Separate repository effect authority from GitHub mutation identity/carrier. Application run `33589668328` is the causal Stage 1D evidence: its Actions `GITHUB_TOKEN` received HTTP 403 on `pull-request-create`, and the target MUST NOT enable `Allow GitHub Actions to create and approve pull requests`. Repository kernel/application derives and fresh-authorizes the exact legal effect plan, including repository, operation, target, base/head/revision and preconditions. A legal event-capable carrier such as the Scheduled-Agent connector / GitHub App executes only that bound plan when Actions cannot legally perform the identity-sensitive PR lifecycle effect or its identity would break required event propagation. The carrier MUST NOT select work, Action, successor, target, effect, retry policy, weaker preconditions, or success semantics. Preserve #58's validated archive-branch success boundary and reuse-first final PR presentation.
- Deterministic application rejection MUST expose a machine-readable exact failed predicate and the relevant expected/observed evidence, rather than only an aggregate `effect precondition rejected`. This is observability of repository-owned authorization; it does not weaken fail-closed behavior or give the carrier workflow authority.
- Before first independent `review-openspec` acceptance, a material formalization defect in the Explore source/evidence/feasibility premise may return the same Issue to Explore after provisional non-`unset` activation while preserving Change, PR/artifact history, evidence and WIP. After acceptance, use resolve-question plus independent review.
- Preserve WIP=1, Human authority, completeness/provenance, role separation, exact-revision gates, semantic evidence, stale/concurrency fail-closed behavior and deterministic archive automation.
- Mandatory N-1 order is **1A exact-revision application resource -> 1B content-addressed work-product ingress/self-hosting -> 1C run-scoped result transport/daily check-in -> 1D identity-sensitive PR mutation authority/carrier split -> 2 kernel shadow -> 3 typed application -> 4 one-Action wake -> 5 Action-only/explicit-merge cutover plus typed retirement of explicitly absorbed pre-activation sources -> 6 deletion/context reduction**. Stage 1A remains the special pre-review prerequisite because the currently selected Propose/Resolve action must obtain exact-`R` strict validation for an already-current handoff revision without dummy artifact writes. Stage 1B formalizes the distinct content-addressed ingress/application-completion boundary bootstrapped by M0 and retains production live-E2E acceptance. Stage 1C remains the mandatory #168 run-scoped transport deployment. Stage 1D is mandatory because the observed Actions PR-create path is not a legal carrier under the preserved environment constraint. Stages 1B–1D must be deployed before Stage 2, but after valid Stage-1A evidence they do not add another semantic OpenSpec review prerequisite. An unmerged or merely green maintenance implementation is evidence of buildability, not deployment or exact-`R` readiness evidence. Each stage states outcomes advanced/still incomplete, N-1 prerequisite, rollback/cutover boundary and continuation; split unsafe stages rather than weaken the parent.
- Completion deletes/disables superseded Role routing, response mailbox/history coupling, Markdown topology/effect parsing, same-wake continuation/wake barriers, obsolete compatibility hot paths, legacy Responses/model-worker host code and redundant machine-control prose/tests. No permanent dual control plane.

## Capabilities

### Modified Capabilities

- `scheduled-agent-workflow`: canonical state/routing, explicit merge Actions, content-addressed work-product self-hosting, run-scoped transport, self-hosted exact-revision validation, typed application, one-action-per-wake, bounded pre-acceptance Explore correction, staged cutover/deletion.
- `repository-governance`: executable kernel becomes the single machine-decidable workflow-semantics owner; Human-readable governance/Skills retain semantic ownership.

## Propose readiness evidence

Merged PR #185 is deployed on N-1 at `main@013510b12c5d3cde869308a319a1e2fb12cdfa60`; its replacement PR preserved exact head `bbc2928839bc0cfac7a1fb383b6ece9e9e1bb291`, and PR #185's exact-head OpenSpec Validate (`33472126991`) and Python Quality (`33472127041`) both passed before merge. That proves the initial bootstrap is buildable and deployed, but fresh current evidence showed it was still too narrow for the approved Stage 1A contract: `prepare_exact_openspec_validation()` created a probe only for `Lead / propose-change`, while #138 required `Lead / resolve-question`.

The Stage 1A blocker was observed at #178 head `52a9a61c74c269ce85b6c8ab4be923fd220c6a9b`: its only observed `OpenSpec Validate` run was `33477885739`, terminal `action_required`, with zero validator jobs. That revision therefore had no qualifying exact-`R` checkout/compatibility/strict-PASS evidence. Subsequent specification-correction commits do not convert that historical failure into validation for a newer head. Review readiness remains fail closed until the final exact #178 handoff revision is validated through the required application-owned Stage 1A resource. The required N-1 correction is gate-derived exact-revision validation that can validate an already-current target head without an artifact rewrite and without an Action whitelist suppressing the resource.

Human-approved M0 conclusion `issuecomment-5521336171` bound the missing self-hosting prerequisite and corrected the formal order. M0 PR #189 merged the bounded unreferenced-blob work-product ingress, application-owned exact tree/single-commit construction, Resolve exact-R validation resource, and application-owned HANDOFF completion. Production live application run `33730731896` then exposed a cross-credential defect: Actions `GITHUB_TOKEN` could not pre-read connector-created unreferenced blobs through `GET git/blobs/{sha}`. Follow-up PR #190 is merged/deployed at `main@e8c3dc7b256bc167217e25a397e98181bdf6f123`; it removes that cross-identity blob-GET prerequisite, makes application-owned `create tree` the first manifest-SHA resolution boundary, requires exact recursive tree path/blob verification before commit creation, and keeps commit/ref/PR/file postconditions fail closed. #189 + #190 are M0 prerequisite/buildability evidence; they do not satisfy Stage 1B or #138 until the production connector-blob → Actions-tree → single-R → exact-R validation path succeeds live.

The #168 transport direction is already decision-complete source evidence through `issuecomment-5442745071` and Human clarification `issuecomment-5475109024`; its production deployment is Stage 1C. The later Actions `pull-request-create` HTTP 403 in application run `33589668328`, preserved by Human clarification `issuecomment-5507379401`, independently proves that Stage 1D must separate repository effect authority from the legal mutation carrier without enabling Actions PR-create/approve permission. Stages 1B–1D block Stage 2, not the current semantic OpenSpec review gate after valid Stage-1A evidence.

## Skill maintenance traceability

Current causal source for every entry: #138 formal-correction Explore `issuecomment-5482546619`; preserved upstream architecture evidence `issuecomment-5474475020`; #168 transport Explore result `issuecomment-5442745071`; Human clarifications `issuecomment-5475109024`, `issuecomment-5477274582`, `issuecomment-5507379401`, and M0/N-1 correction `issuecomment-5521336171`; M0 live application failure run `33730731896`; M0 follow-up PR #190; Actions PR-carrier failure run `33589668328`; Change `simplify-scheduled-agent-control-plane`.

| Skill | Class | Before / preserved responsibility | After / rationale / supersession |
| --- | --- | --- | --- |
| `openspec-explore` | Modified | Research/evidence, feasibility/materiality, Human boundary, Explore dispositions; also routing/continuation mechanics. | Preserve semantic Explore work; kernel/application supersede routing, successor and wake mechanics. |
| `openspec-change` | Modified | Proposal/spec/design/tasks, causal Explore verification, scope/contract, traceability/readiness; also application/routing mechanics. | Preserve Lead specification authority and exact-revision readiness; add approved pre-acceptance Explore correction; deterministic application/validation self-host the exact-revision resource while kernel/application supersede machine control. |
| `openspec-review` | Modified | Independent source-chain + reverse/forward semantic review, Skill/config checks and validation; also handoff mechanics. | Preserve independent PASS/FINDINGS and exact-target validation verification; kernel owns structural Action/successor selection. |
| `implementation` | Modified | Approved Apply context, RED-GREEN-REFACTOR-VERIFY, checkpoints, spec blockers; current continuation semantics. | Preserve implementation/testing/checkpoints inside `implement-change`; application ends the wake and supersedes successor execution. |
| `implementation-review` | Modified | Exact-head conformance/tests/gates/Skill trace and finding class; PASS -> generic merge. | Preserve exact-head independent gate; PASS feeds `merge-implementation-pr`; generic merge Action is superseded here. |
| `archive-review` | Modified | Exact Archive head/canonical/preparation/cleanup-retention gate; PASS -> generic merge. | Preserve Archive gate; PASS feeds `merge-archive-pr`; generic merge phase inference is superseded here. |
| `merge-pr` | Modified | Generic Action infers phase while enforcing exact PASS/head/check/linkage/native-closing/preparation/cleanup/Human safety. | Keep one shared merge procedure for two explicit merge Actions and all applicable safety gates; only generic Action identity/phase inference is superseded here. |
| `lifecycle-finalize` | Modified | Post-merge lifecycle, archive automation/PR preparation, trackers/cleanup, terminal close; also finite routing details. | Preserve Lead lifecycle/archive/terminal judgment; kernel/explicit merge Actions supersede finite routing/continuation mechanics. |

No Skill is removed/replaced and no new Skill is added. `skill-creator` remains guidance only and is not materially modified. The generic `merge-pr` **Action** is superseded; the `merge-pr` Skill remains the shared merge procedure.

## Impact

Affected surfaces: shared governance, `agents/workflow.md`, the eight Skills above, message references, both capability specs, and dispatcher/runtime/worker/effect/transport/application/validation/carrier code plus production-boundary tests. Deterministic rejection observability is part of the application boundary. Final production must have fewer independent control-state representations and executable decision paths than current `main`.

Refs #58
Refs #138
Refs #168
