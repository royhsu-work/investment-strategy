# Tasks

## 1. Dispatch mode and workflow-first role selection

- [x] 1.1 RED: add behavioral tests for the authoritative `Scheduled-Dispatch-Mode` marker, fixed-role compatibility, workflow-dynamic active-workflow selection, invalid/multiple-active fail-closed behavior, and immutable invocation role; verify failures are caused by missing target behavior.
- [x] 1.2 GREEN: implement the minimum default-branch governance/dispatcher behavior that parses the single marker and derives role/action/skill from the one active workflow without global urgency scoring or a second DAG.
- [x] 1.3 REFACTOR/VERIFY: keep bootstrap logic thin; run slice tests, full regression suite, type checks, and lint checks.

Trace: proposal dispatch mode + thin dispatcher → specs `Default-branch governance declares the scheduled dispatch mode`, `Workflow-dynamic dispatch derives one fixed invocation role`, modified `Each scheduled run processes...` → design Decisions 1-2.

## 2. Single-active activation and concurrent wake safety

- [x] 2.1 RED: add tests for `Change: unset` queued proposals, persisted Change activation, oldest-created/lower-number activation order, refusal to activate while another Change is active, and overlapping/stale activation attempts; verify target-behavior RED failures.
- [x] 2.2 GREEN: implement the minimum single-active admission/activation behavior using durable reconstruction and first-valid-write-wins/precondition checks where applicable, without lock/claim/lease/heartbeat/in-progress state.
- [x] 2.3 REFACTOR/VERIFY: confirm existing at-least-once action safety remains intact; run slice tests, full regression suite, type checks, and lint checks.

Trace: proposal activation/concurrency → specs `Persisted Change identity defines...`, `Dynamic dispatch tolerates...`, modified Human admission → design Decisions 3-4.

## 3. Orphan guard and Human authority/escalation

- [x] 3.1 RED: add tests for unexplained durable workflow evidence blocking new activation, Human-required evidence accepted only from `royhsu-work`, non-Human evidence remaining non-authoritative, `human:notified` having no routing/authorization effect, and duplicate unanswered escalation suppression.
- [x] 3.2 GREEN: implement the minimum Lead diagnosis/escalation and actor-bound Human evidence checks required by the specs; do not add a generic fault classifier or Human waiting state machine.
- [x] 3.3 REFACTOR/VERIFY: ensure Human-facing escalation is decision-ready with at most three options, impact/trade-off, and recommendation; run slice tests, full regression suite, type checks, and lint checks.

Trace: proposal orphan/Human contract → specs `Unexplained durable workflow evidence...`, `Human-required authority...`, `Lead Human-facing escalation...` → design Decisions 5-6.

## 4. Verified-slice coordination checkpoint

- [x] 4.1 RED: add contract/regression coverage proving a successfully verified Executor slice requires both satisfied task markers and one bounded persistent-coordination-Issue checkpoint before another slice or handoff; cover reconstruction where markers are durable but the checkpoint write was interrupted.
- [x] 4.2 GREEN: update shared governance and `Executor / implement-change` skill guidance so each verified slice checkpoint records completed slice/task IDs, durable checkpoint or verified revision, VERIFY/gate result, and remaining work or handoff; preserve PR/CI/task evidence as their own sources of truth.
- [x] 4.3 REFACTOR/VERIFY: confirm checkpoint journaling occurs only at verified completion boundaries and introduces no heartbeat, progress percentage, `status:in-progress`, lock/claim/lease, retry counter, or other live runtime state; run slice tests, full regression suite, type checks, and lint checks.

Trace: proposal verified-slice observability → spec `Verified implementation slices persist a bounded coordination-Issue checkpoint` and repository-artifact bootstrap boundary → design Decision 10.

## 5. Lifecycle-transition journal and native-close terminal handoff

- [x] 5.1 RED: add contract/regression coverage proving material workflow lifecycle transitions require one bounded persistent-coordination-Issue journal record while ordinary RED/GREEN/refactor/test-trigger/compatibility-correction commits and artifact/task edits inside an unverified implementation slice do not; cover interrupted lifecycle-journal recovery without repeating the completed transition.
- [x] 5.2 RED: add lifecycle/dispatcher coverage for final Archive PR native close: after authorized archive merge, Executor observes the closed Issue, replaces consumed routing with `agent:lead + action:finalize-archive`, journals the merge/native-close/handoff lifecycle boundary, and ends without role switching; cover interrupted post-merge handoff recovery without duplicate merge.
- [x] 5.3 RED: add selection/finalization coverage proving `closed + Lead / finalize-archive` is eligible only while matching authorized merged-archive/native-close evidence exists and no valid Lead `LIFECYCLE_COMPLETE` result exists; prove terminal-pending work blocks queued activation and completed terminal history does not.
- [x] 5.4 GREEN: update shared governance, dispatcher/action skills, and lifecycle documentation to implement lifecycle-transition journaling, the closed terminal-reconstruction exception, Executor post-merge terminal handoff, and Lead bounded `LIFECYCLE_COMPLETE` evidence without adding per-mutation implementation logging, a completion/status label, or reopening normally natively closed Issues.
- [x] 5.5 REFACTOR/VERIFY: confirm implementation progress comments remain exactly one per verified Slice, lifecycle journal entries occur only at material ownership/state transitions, and canonical completion still requires authorized Archive PR merge + correct archived default-branch state + observed closed Issue; confirm no new lifecycle action, heartbeat/progress/in-progress state, lock/claim/lease, or generic terminal state machine was introduced; run slice tests, full regression suite, type checks, and lint checks.

Trace: proposal Slice-boundary observability + lifecycle-transition journal + native-close terminal ownership → specs `Verified implementation slices persist...`, `Material workflow lifecycle transitions are journaled...`, modified `Actionable workflow routing is one logical role/action tuple`, `Native Archive close hands off...`, modified active-workflow/work-selection requirements → design Decisions 3, 10-12.

## 6. Shared work-conserving action termination semantics

- [x] 6.1 RED: add contract/regression coverage proving a selected action continues immediately actionable work in the same invocation while routing, revision/preconditions, authority, and execution context remain current. Cover at least a failed-but-actionable validation and a verified Executor Slice checkpoint with more approved work; prove neither is a voluntary yield point. Also cover legal termination for handoff, genuine external asynchronous wait, and stale/concurrency loss.
- [x] 6.2 GREEN: make `agents/AGENTS.md` the single source of truth for the shared work-conserving continuation/termination contract. Remove or narrow action-specific role/skill wording that duplicates or weakens the shared rule, including `implement-change` language that treats remaining approved work as an automatic `continue on a later run` outcome. Preserve only action-specific blockers/results/handoffs and actual crash-recovery semantics.
- [x] 6.3 REFACTOR/VERIFY: confirm the nine action skills do not carry competing generic voluntary-yield policies; confirm recoverable same-role failures remain same-invocation work, real external waits remain legal, routing handoff still ends the invocation without role switching, and stale/concurrency loss still terminates safely. Run slice tests, full regression suite, type checks, and lint checks.

Trace: proposal shared work-conserving selected-action contract → spec `Selected Scheduled Agent actions are work-conserving within an invocation` and modified repository-agent-artifact requirement → design Decision 13.

## 7. Lead systemic coherence, idle advisory, Reviewer order, documentation, and migration contract

- [x] 7.1 RED: add tests/contract checks for the Lead role's bounded systemic-coherence responsibility: when a material workflow/specification defect plausibly indicates a cross-cutting pattern, Lead must inspect the directly related sibling actions/contracts before fixing only the first symptom; contract coverage must also reject continuous supervision/progress polling or unrelated broad audits as part of that responsibility. In the same slice, cover the seven-day relevant-Issue advisory lens, bounded/no-duplicate advisory behavior, required reverse-first `review-openspec` inspection followed by forward inspection with unchanged exact-revision bidirectional PASS semantics, bootstrap-only Scheduled Task contract, and simplicity/proportionality constraints where mechanically testable.
- [x] 7.2 GREEN: update `agents/roles/lead.md` to own the systemic-coherence responsibility once, requiring bounded root-cause/blast-radius analysis and narrowest-correct-layer selection for plausible cross-cutting defects without granting supervisory authority over another role's valid routed work. Do not duplicate this responsibility in `agents/AGENTS.md` or each Lead skill. Also update repository governance/docs/role-skill guidance needed for workflow-dynamic mode and Reviewer reverse-first `review-openspec`; switch the canonical dispatch marker only with completed compatible behavior; document the external migration plan retaining the existing three wake slots while keeping exact slot count/topology/cadence outside repository capability/runtime state, and document the common external bootstrap-prompt contract.
- [x] 7.3 REFACTOR/VERIFY: confirm Lead systemic-coherence scanning is bounded to the plausible blast radius and does not become progress polling, unrelated repository-wide audit, or a central orchestration layer; confirm reverse-first changes inspection order only and does not weaken bidirectional traceability; confirm the retained three-slot topology remains an external migration/product-configuration constraint rather than repository workflow state; confirm no global priority table, multi-active arbitration, fault platform, lock/lease/claim state, duplicate termination policy, or duplicate OpenSpec DAG was introduced; run slice tests, full regression suite, type checks, and lint checks.

Trace: the Lead systemic-coherence portion is an Engineering/Governance role-artifact responsibility traced from proposal role responsibility → design Decision 14 → this slice → `agents/roles/lead.md` under the `openspec/config.yaml` governance-task allowance; it is intentionally not duplicated as a separate generic capability requirement. The behavior/product portions of section 7 trace from proposal idle/reviewer-order/simplicity intent → specs idle, `OpenSpec review uses reverse-first inspection while retaining the bidirectional gate`, repository artifacts, and proportionality requirements → design Decisions 7-9. The exact three-wake-slot preservation is a migration/governance verification traced to proposal scope + design `Scheduled Task migration` + the `openspec/config.yaml` allowance for Engineering/Governance tasks; it is intentionally not a repository capability requirement.

## 8. Historical OpenSpec readiness gate

- [x] 8.1 Historical evidence: the earlier contract revision performed reverse and forward trace inspection before independent review.
- [x] 8.2 Run repository-pinned `openspec validate --all --strict --json --no-interactive` against the exact final change revision and preserve exact-checkout validation evidence.
- [x] 8.3 Historical evidence: the earlier contract revision performed author-side scope/coherence readiness checks before independent review.

Section 8 records historical completion evidence for the contract that existed before later Human clarifications. Its old author-side semantic-review wording is superseded by the authoritative Human clarification on #25: Lead authors/maintains trace references, Reviewer owns the independent semantic bidirectional traceability gate, and Executor does not execute that semantic gate. Section 8 MUST NOT be reused as normative role ownership for current or future implementation.

## 9. Canonical workflow messages, durable handoff completion, and Human delivery boundary

- [x] 9.1 RED: add contract/regression coverage for one shared `agents/templates/messages.md` source with the common workflow envelope and exactly the eight approved recurring message types: `ACTION_RESULT`, `REVIEW_RESULT`, `SLICE_CHECKPOINT`, `MERGE_AUTHORIZATION`, `MERGE_RESULT`, `HANDOFF`, `HUMAN_DECISION_REQUIRED`, and `EXECUTION_EXCEPTION`. Cover required event-specific fields derived from #18/#21/#25 and the demonstrated catchable execution-failure case; prove role/skills reference the shared templates instead of copying template bodies, and prove free-form Lead progress polling, RED/GREEN/test-trigger/compatibility-correction progress, and `No Human action is required` noise are not supported message types.
- [x] 9.2 RED: add lifecycle/recovery coverage proving a persisted action/review result does not itself complete a required routing handoff. Cover at least `Executor / implement-change` READY with routing still on Executor: recovery must preserve completed Slice/result evidence, fresh-read the source tuple, perform only the missing routing mutation to `Reviewer / review-implementation`, then persist canonical `HANDOFF` evidence without repeating implementation or fabricating another result. Cover the same shared invariant for a representative Reviewer or Lead handoff, and prove canonical typed boundary messages satisfy the existing lifecycle-journal requirement without an additional duplicate generic meta-journal.
- [x] 9.3 GREEN: create `agents/templates/messages.md` as the single Markdown presentation contract and update shared governance plus the applicable role/skill documents to reference the correct template at each recurring durable event. `EXECUTION_EXCEPTION` must preserve raw platform-observable error text separately from classification/disposition and include the bounded factual operation/revision/mutation/work-boundary fields. Preserve action-specific result enums and authority in their existing contracts; templates must not redefine routing, authorization, termination, review, merge, lifecycle, or global exception meaning and must not introduce a parser-dependent message bus, JSON/YAML runtime schema, template engine, notification state, generic exception engine, or hidden workflow state.
- [x] 9.4 GREEN: implement/document the Human-delivery eligibility boundary: ordinary Reviewer/Executor workflow messages, all-role `EXECUTION_EXCEPTION` evidence, and ordinary Lead results remain repository-durable evidence only; only Lead `HUMAN_DECISION_REQUIRED` is Human-facing delivery-eligible, and only when Lead cannot resolve the required Human authority/intent from current approved contract and durable evidence. Update the external Scheduled Task migration/bootstrap documentation so ordinary wakes are default-silent while actual product notification/associated-conversation surfacing remains external product configuration rather than repository state. Do not emit `No Human action is required` messages as a substitute for silence.
- [x] 9.5 REFACTOR/VERIFY: confirm the eight templates cover the recurring durable message families and newly demonstrated catchable exception evidence without adding per-action template copies or a generic `LIFECYCLE_JOURNAL`; confirm `EXECUTION_EXCEPTION` is evidence rather than an action result/lifecycle transition by default; confirm `SLICE_CHECKPOINT` remains completion-boundary observability rather than live progress; confirm `HANDOFF` follows successful routing mutation and result-before-handoff interruption is recoverable without replay; confirm only unresolved Lead Human decisions are delivery-eligible. Run focused tests, full regression suite, type checks, lint checks, and current OpenSpec validation required by the slice.

Trace: proposal canonical messages + result-vs-handoff + Human-delivery boundary + raw exception evidence shape → specs `Recurring workflow messages use canonical shared templates`, `Catchable execution exceptions preserve raw observable evidence before disposition`, modified `Routing handoff persists evidence before ownership transfer`, `Human-facing scheduled delivery is Lead-only and decision-required`, modified verified-Slice/lifecycle-journal/repository-artifact requirements → design Decisions 11 and 15-18.

## 10. Shared exception capture and invocation finalization

- [ ] 10.1 RED: add shared contract/regression coverage proving every Scheduled Agent role inherits catchable tool/runtime/execution exception capture. Cover representative Lead, Reviewer, and Executor operations; require canonical `EXECUTION_EXCEPTION` to preserve the platform-observable raw error exactly as returned after existing safety redaction, keep interpretation/classification separate, allow `UNCLASSIFIED_EXECUTION_EXCEPTION`, identify operation/tool + relevant revision/base + known durable mutation outcome + unfinished work boundary, and prohibit classification-only/paraphrase-only evidence.
- [ ] 10.2 RED: add disposition/finalization coverage proving a captured exception that is legally recoverable within the same current role/action is repaired and work-conserving execution continues without voluntary yield. Also cover a catchable failure with no legal local disposition: completed work is preserved, bounded diagnosis routes to `Lead / resolve-question`, and the required result/routing/HANDOFF boundary is completed rather than leaving only an external Scheduled Task reply. Cover truly uncatchable hard termination as a later at-least-once reconstruction case without fabricated exception evidence.
- [ ] 10.3 GREEN: make `agents/AGENTS.md` the single source of truth for shared exception capture and normal invocation finalization. Integrate the contracts with the existing work-conserving and handoff rules: catch first when possible, persist raw evidence, recover-and-continue when legal, otherwise converge to an action-defined durable disposition and required handoff before normal exit. Do not create a universal blocked result, global error taxonomy, retry engine/counter, automatic remediation platform, hidden failure state, or second orchestration layer.
- [ ] 10.4 GREEN: update the role/skill/documentation surfaces only as needed so all nine actions inherit the shared contract without copying it. Keep action-specific normal results and known local recovery in their existing skills; when a newly observed catchable failure has no legal action-specific path, route bounded diagnosis to Lead using the captured evidence. Scheduled Task prompts remain bootstrap-only and MUST NOT duplicate the exception/finalization protocol.
- [ ] 10.5 REFACTOR/VERIFY: confirm raw exception evidence is safety-bounded, classification remains evidence-based and optional, local recovery does not yield, normal non-local catchable failure exits are reconstructable, uncatchable termination still relies on later reconstruction, and no generic fault/retry framework or duplicate per-skill execution contract was introduced. Run focused tests, full regression suite, type checks, lint checks, and current OpenSpec validation required by the slice.

Trace: proposal shared exception capture + invocation finalization → specs `Catchable execution exceptions preserve raw observable evidence before disposition`, `Catchable execution exceptions are dispositioned before normal invocation exit`, shared work-conserving requirement, modified canonical handoff and repository-artifact requirements → design Decisions 13, 18-19.

## 11. Traceability ownership correction and implementation completion

- [ ] 11.1 RED: add governance/contract coverage proving responsibility separation. `agents/roles/lead.md`, `agents/skills/openspec-change/SKILL.md`, and shared `agents/AGENTS.md` must require Lead to author/maintain required trace declarations/references but must not assign Lead the semantic bidirectional PASS gate. `agents/roles/reviewer.md` and `agents/skills/openspec-review/SKILL.md` must retain Reviewer-owned reverse-first then forward semantic verification and exact-revision PASS/FINDINGS authority. README lifecycle presentation must place bidirectional semantic review inside `Reviewer / review-openspec`. Executor completion/task guidance must not require Executor to decide proposal/spec/design/task semantic coherence. Preserve `openspec/config.yaml` authoring trace rules and mechanical strict-validation rules unchanged.
- [ ] 11.2 GREEN: correct the bounded ownership leakage in `agents/roles/lead.md`, `agents/skills/openspec-change/SKILL.md`, `agents/AGENTS.md`, and README lifecycle wording. Lead readiness should require required artifacts, required trace declarations/references, and valid exact-revision mechanical OpenSpec validation evidence; it must hand the semantic bidirectional gate to Reviewer. Do not weaken Reviewer independence or remove the existing Section 7 tests that implement the Reviewer reverse-first contract. Do not move strict `openspec validate` to Reviewer-only ownership.
- [ ] 11.3 REFACTOR/VERIFY: run focused governance tests, full regression suite, type checks, lint checks, and repository-pinned exact-head strict OpenSpec validation. Confirm implemented artifacts stay within the approved task/spec boundaries and introduce none of the explicitly rejected machinery. Executor MUST NOT perform or claim semantic bidirectional OpenSpec review as part of this completion step; the next independent `Reviewer / review-openspec` gate owns semantic traceability verification for the exact revised OpenSpec revision.

Trace: authoritative Human clarification on #25 + existing spec `OpenSpec review uses reverse-first inspection while retaining the bidirectional gate` + existing `openspec/config.yaml` authoring trace rules → design Decision 9 and role-ownership boundaries → implementation slice 11. The correction removes responsibility leakage without creating a new capability or review layer.

## 12. Authoritative context/provenance continuity and cumulative review coverage

- [ ] 12.1 RED: add shared governance/contract coverage proving all nine Scheduled Agent actions reconstruct still-applicable durable evidence instead of treating the newest comment, readiness result, handoff, routing transition, validation result, or revision as an implicit replacement for earlier unresolved obligations. Cover explicit contract-defined consumption through authoritative supersession, durable resolution, applicable independent gate acceptance, lifecycle completion, or another action-specific legal consumption event, and prove simple recency alone does not consume evidence.
- [ ] 12.2 RED: add cross-Issue provenance and Reviewer-coverage regression tests. Use the demonstrated #23 → #25 shape to prove a coordination Issue that declares authoritative upstream decision/gate references causes Lead authoring and `review-openspec` to dereference those sources rather than trusting a shortened Issue summary as replacement authority. For `review-openspec`, `review-implementation`, and `review-archive`, prove Reviewer reconstructs the last valid independent review baseline B and exact current target R, covers every material unreviewed change in `(B, R]`, and still evaluates the complete current state at R; an intermediate readiness/handoff/mechanical validation or unreviewed revision must not advance B or erase pending coverage.
- [ ] 12.3 GREEN: make `agents/AGENTS.md` the single source of truth for shared authoritative-context continuity. Update `agents/skills/openspec-change/SKILL.md` so Lead dereferences declared upstream authoritative decision/gate provenance during `propose-change` and materially revised `resolve-question`. Update `agents/roles/reviewer.md` with the Reviewer-wide revision-coverage invariant and specialize `openspec-review`, `implementation-review`, and `archive-review` reconstruction for their applicable accepted baseline/current target. Other Lead/Executor skills should only reconstruct the still-applicable evidence their existing action needs and MUST NOT duplicate a generic context framework.
- [ ] 12.4 GREEN: update README/governance documentation to distinguish current snapshot semantics from unresolved durable-evidence semantics, explain that a cross-Issue summary is orientation rather than replacement authority, and document the Reviewer `B → R` cumulative-coverage rule without adding a message queue, event-sourcing engine, hidden context cache, sequence number/label, pending-review state, consumed-evidence flag, or second workflow DAG.
- [ ] 12.5 REFACTOR/VERIFY: run focused continuity/provenance/reviewer-baseline tests, the full regression suite, type checks, lint/format checks, and repository-pinned exact-head strict OpenSpec validation. Confirm the correction applies across all nine actions through shared reconstruction governance, preserves Reviewer independence and exact-current-state review, and introduces no generic event/context processor or persistent consumption state.

Trace: authoritative Human clarification on #25 + demonstrated #23 → #25 source-context loss + demonstrated unreviewed `ea8aaa6...` before later `a225dbf...` target → specs `Scheduled Agent reconstruction preserves authoritative context continuity` and `Revision-bound Reviewer gates preserve cumulative unreviewed coverage` → design Decision 20 → implementation slice 12.
