# Tasks: Qualify active formal consequences

## Delivery stage 1 — One independently reviewable implementation stage

Parent outcome: correctness-critical active formal state participates in dispatch and consequence only after affirmative repository-owned qualification, while legitimate Change: unset intake and semantic Human/Role boundaries remain intact.

N-1 prerequisites: current default-branch governance and OpenSpec configuration; the finite Action/Role/Result model; current complete GitHub Issue reconstruction and dispatch preflight; existing ObservationProvenance; Actions-authored formal evidence recognition; application reauthorization; current routing/terminal postcondition observation; CarrierRequired and exact carrier plans; and the existing quality/strict OpenSpec validation harness.

Stage boundary: implement only the canonical owner rule, the concrete active-formal qualification at current reconstruction/dispatch and application boundaries, and the regressions required by the acceptance boundary. Do not add a new workflow state, registry, transport, carrier protocol, or policy engine.

Stage exit criteria: all slices below are implemented and verified at one exact revision; direct/equality-only formal state fails closed; repository-owned qualified state proceeds; Change: unset compatibility and current selection semantics are preserved; full quality and strict OpenSpec validation are green; and no unrelated Issue or product scope is present. Review, merge, archive, and terminal operations remain lifecycle gates.

## Slice 1 — Establish the qualification fixtures and owner boundary

Trace: proposal acceptance boundary -> repository-governance owner rule -> existing ObservationProvenance and current-state fixtures.

- [ ] 1.1 RED: add deterministic fixtures for Change: unset pre-activation intake and for active formal observations with qualified, unqualified, missing, stale, ambiguous, contradictory, incomplete, and equality-only evidence.
- [ ] 1.2 RED: add a regression proving the canonical owner rule is represented once and the scheduled workflow consumes the concrete owner rather than a second acceptance shortcut.
- [ ] 1.3 GREEN: extend the existing current-state observation construction to carry affirmative active-formal qualification through ObservationProvenance without persisting new workflow state.
- [ ] 1.4 REFACTOR: keep legitimate Human-requested, idle-discovery, scope-split, and Change: unset Propose/Explore intake on the existing pre-activation path; remove duplicate helper or prose authority if introduced.
- [ ] 1.5 VERIFY: run focused observation/dispatch tests, the full Python suite, Ruff check, Ruff format check, mypy, and strict OpenSpec validation.

## Slice 2 — Bind repository-owned evidence to the consequence

Trace: scheduled-agent-workflow modified requirement -> existing Actions-authored evidence/parser -> exact application postcondition.

- [ ] 2.1 RED: add a regression proving that a repository-owned authorization and accepted Action/Result with matching source/default-branch revisions, model-derived next_action, and exact durable route or terminal postcondition is QUALIFIED.
- [ ] 2.2 RED: add regressions proving that connector-authored or merely structural result/route data, and result/route data with missing, stale, contradictory, ambiguous, or incomplete bindings, is INDETERMINATE.
- [ ] 2.3 GREEN: reuse the existing GitHub Actions author check, formal marker/field parser, application authorization, executable transition model, and postcondition observation to implement the single concrete qualification predicate.
- [ ] 2.4 GREEN: ensure exact carrier-plan completion can be reconstructed by a later fresh wake as repository-owned evidence, while the carrier remains an actuator without workflow authority.
- [ ] 2.5 VERIFY: run focused provenance/postcondition/carrier regressions and all current quality checks.

## Slice 3 — Enforce qualification before dispatch and application consequence

Trace: current-state qualification -> deterministic dispatch -> application/effect guard.

- [ ] 3.1 RED: add a regression proving that a syntactically valid active route or terminal close fails closed before dispatch when no affirmative qualification exists, even when the current route equals the requested target.
- [ ] 3.2 GREEN: make current active-formal qualification part of the existing dispatch preflight; preserve complete-enumeration, formal-priority, single-active-WIP, finish-first, and no-fallback semantics.
- [ ] 3.3 RED: add an application regression proving that the existing equality-only _formal_transition_is_qualified path cannot accept an out-of-band route or terminal transition.
- [ ] 3.4 GREEN: remove the equality-only acceptance shortcut and make derived routing/terminal effects consume the single qualified evidence path; preserve idempotent postcondition observation only after qualification.
- [ ] 3.5 VERIFY: run focused dispatch/application/terminal regressions, full Python quality, and strict OpenSpec validation.

## Slice 4 — Preserve compatibility and close the exact acceptance boundary

Trace: Human phase distinction -> current finite model -> regression and repository quality boundary.

- [ ] 4.1 RED: add integration regressions for Change: unset legitimate intake, active formal repository-owned success, direct mutation failure, carrier later-wake qualification, formal priority/WIP, finish-first, no-rewind, and no-fallback.
- [ ] 4.2 GREEN: reconcile all integration behavior through existing Action/Result/application/carrier/postcondition owners; do not add a phase, registry, ledger, cursor, mailbox, retry state, new protocol, or second DAG.
- [ ] 4.3 REFACTOR: remove redundant qualification or fallback code and keep openspec/config.yaml unchanged unless executable authoring evidence proves a narrowly necessary guidance gap.
- [ ] 4.4 VERIFY: run focused and full tests, Ruff check, Ruff format check, mypy, strict OpenSpec validation, and exact repository checks at the final implementation revision.
