# Tasks — integrate-taiwan-eod-market-data

## 1. Venue-aware market-data identity and pre-load configuration failure

- [ ] 1.1 RED — Add outside-in Decision and analytical Backtest behavior tests for configured `TWSE` and `TPEX` instruments proving market-data loading receives a provider-neutral identity containing canonical symbol + listing venue while StrategyContext still exposes only the canonical symbol. Trace: proposal instrument identity; `strategy-engine` listing-venue requirements; design Decisions 1–2.
- [ ] 1.2 RED — Add configuration behavior tests for missing listing venue and unsupported/provider-specific venue values, expecting `CONFIGURATION_FAILED / LISTING_VENUE_NOT_CONFIGURED` or `CONFIGURATION_FAILED / UNSUPPORTED_LISTING_VENUE`; prove market-data loading and Strategy evaluation do not run. Run first and verify failures are caused by missing venue behavior.
- [ ] 1.3 RED — Add YAML registry tests proving `TWSE` and `TPEX` load as provider-neutral venue identity and remain independent of active strategy assignment, including an instrument with valid venue and no active assignment.
- [ ] 1.4 GREEN — Implement immutable market-data instrument identity, extend instrument/YAML configuration with optional listing venue, validate supported venues during configuration resolution, carry the identity in resolved application configuration, and update MarketDataGateway/data-preparation boundaries to consume it.
- [ ] 1.5 REFACTOR — Keep listing venue out of StrategyContext and Strategy-specific inputs; keep YAML/provider ticker syntax out of domain behavior; concrete adapters must not query InstrumentRegistry directly.
- [ ] 1.6 VERIFY — Run slice tests, then `uv run pytest`, `uv run ruff check .`, `uv run ruff format --check .`, and `uv run mypy src tests`.

## 2. Reported completed-daily OHLCV through a replaceable provider adapter

- [ ] 2.1 RED — Add adapter tests for one TWSE and one TPEx instrument proving provider-specific mapping occurs only inside the concrete adapter (`00733 + TWSE -> 00733.TW`, `00679B + TPEX -> 00679B.TWO`) and the boundary returns plain candidate records rather than provider SDK/DataFrame types. Trace: `taiwan-eod-market-data` provider isolation; design Decision 3.
- [ ] 2.2 RED — Add an injected/fake history-loader test that captures invocation arguments and proves daily reported history is explicitly requested with automatic adjustment, back-adjustment, and repair disabled; prove `Adj Close`, actions, and repair metadata are not substituted into formal OHLCV.
- [ ] 2.3 RED — Add candidate-conversion tests proving timestamp/open/high/low/close/volume pass through without interpolation or synthetic filling; missing/invalid candidate fields remain visible to existing normalization/structural validation.
- [ ] 2.4 RED — Add acquisition tests proving adapter exception or empty response reaches `DATA_FAILED / DATA_UNAVAILABLE`, while acquired invalid observations retain structural failure codes and never become `NEUTRAL`.
- [ ] 2.5 GREEN — Add the yfinance EOD infrastructure adapter and dependency with explicit behavior equivalent to `period=max`, `interval=1d`, `prepost=false`, `auto_adjust=false`, `back_adjust=false`, `repair=false`, `actions=false`, `rounding=false`, and exception-raising behavior; convert output to plain candidate mappings.
- [ ] 2.6 REFACTOR — Isolate all yfinance/pandas imports, ticker suffix rules, response-column handling, and provider exceptions under infrastructure; document/test that `period=max` is acquisition breadth only and does not define analytical or continuity scope.
- [ ] 2.7 VERIFY — Run adapter/price-basis/acquisition tests plus full pytest/ruff/mypy checks; no default test may require live external network access.

## 3. Formal-range continuity and freshness remain independent from provider breadth

- [ ] 3.1 RED — Add validation behavior tests where provider candidates contain an older gap before an explicitly selected formal historical range, while the selected formal range is complete through `resolved_as_of`; prove the older unrelated gap does not produce `DATA_GAP`. Trace: `market-data-validation` provider-breadth requirement; design Decision 4.
- [ ] 3.2 RED — Add a paired test where a trading-day observation is missing inside an explicitly selected formal historical range; prove continuity still fails with `DATA_FAILED / DATA_GAP`.
- [ ] 3.3 RED — Add freshness tests proving the latest required completed trading day must equal `resolved_as_of` regardless of extra older history; missing latest data remains `DATA_FAILED / STALE_DATA`.
- [ ] 3.4 RED — Add an equivalence test proving two provider responses with identical selected formal history but different extra older observations produce equivalent data eligibility/Strategy input.
- [ ] 3.5 GREEN — Refactor combined continuity/freshness behavior so freshness remains tied to `resolved_as_of`, while continuity receives an explicit formal range (or equivalent selected history) and no longer infers its start from the provider's first acquired bar.
- [ ] 3.6 REFACTOR — Do not introduce a Strategy-specific lookback/history-window contract in this change; if no analytical caller has selected a formal continuity range, the EOD layer must not invent one from provider response breadth.
- [ ] 3.7 VERIFY — Run continuity/freshness/regression tests plus full pytest/ruff/mypy checks.

## 4. Taiwan regular-securities TradingCalendar with engine-supported coverage

- [ ] 4.1 RED — Add timezone-aware Taiwan-calendar tests proving `market_date()` uses `Asia/Taipei` and is independent of runner/local timezone. Trace: `taiwan-eod-market-data` timezone/calendar requirements; design Decisions 6–10.
- [ ] 4.2 RED — Add deterministic regression fixtures backed by official TWSE/TPEx regular-market evidence covering at least a normal weekday, weekday exchange holiday, historical additional Saturday session, and exceptional full-market closure; these fixtures validate representative engine behavior and do not constitute a duplicate full historical calendar.
- [ ] 4.3 RED — Add previous/latest-completed-session tests before and after the conservative `13:33 Asia/Taipei` completion boundary.
- [ ] 4.4 RED — Add calendar-coverage tests proving a required date/range that the configured pinned XTAI engine cannot answer because it is outside its actual supported bounds or otherwise unavailable fails with `DATA_FAILED / CALENDAR_UNAVAILABLE` before `DATA_GAP` or `STALE_DATA`; do not invent separate hard-coded earliest/latest project dates.
- [ ] 4.5 RED — Add override-precedence contract tests using deterministic injected/fixture engine behavior: when engine truth and an explicit repository override disagree for the same date, the override wins; when no override exists, the engine answer is used. A real production override entry is not required by this task unless Task 4.2 or other official evidence identifies an actual XTAI discrepancy.
- [ ] 4.6 GREEN — Implement a replaceable Taiwan regular-securities TradingCalendar adapter using a pinned `exchange_calendars` XTAI engine, `Asia/Taipei`, the conservative current-day completion rule, and engine-supported coverage semantics. The adapter may inspect/expose the engine's actual session bounds internally; dates the engine cannot establish map to `CALENDAR_UNAVAILABLE`.
- [ ] 4.7 GREEN — Implement sparse repository override support and precedence. Add production override entries only for real officially verified TWSE/TPEx regular-market discrepancies found during implementation; if none are found, keep the production override set empty rather than fabricating one to satisfy the task.
- [ ] 4.8 REFACTOR — Keep external calendar-library types/version-specific APIs inside infrastructure; do not maintain a duplicate full calendar, do not require per-date official certification across the engine range, and do not add automatic future/exceptional-session discovery, International Bond Market, or Emerging Stock Board semantics.
- [ ] 4.9 VERIFY — Run Taiwan-calendar/session/coverage/override tests plus full pytest/ruff/mypy checks.

## 5. Decision/Backtest integration, baseline regressions, and OpenSpec completion

- [ ] 5.1 RED — Add an outside-in Decision composition test using configured Taiwan venue, concrete provider adapter with injected deterministic EOD data, Taiwan TradingCalendar, fixed Clock, and test-only Strategy; expect successful evaluation from completed EOD facts with no intraday/realtime value entering Strategy evaluation. Trace: proposal existing-service consumption; design Decisions 1, 3, 9–12.
- [ ] 5.2 RED — Add an analytical Backtest composition test over a historical Taiwan range proving the same provider/calendar path preserves chronological replay, WARMUP/no-look-ahead behavior, and no provider-specific fields enter StrategyContext.
- [ ] 5.3 RED — Add integration cases proving incomplete current session is not inserted as a formal daily bar, reliable missing latest session remains `STALE_DATA`, and engine-unavailable calendar status is not rewritten as `DATA_GAP`/`STALE_DATA`.
- [ ] 5.4 BASELINE GREEN — Add/retain regression assertions that Decision and analytical Backtest public request/artifact schemas and exact disclaimer remain unchanged. These assertions must be GREEN before Taiwan composition implementation and remain GREEN afterward; they are not used to manufacture a RED state.
- [ ] 5.5 GREEN — Update Decision/Backtest data-loading orchestration only as needed to pass provider-neutral market-data identity, separate freshness from explicit-range continuity, and consume the Taiwan calendar; reuse existing normalization, Strategy evaluator, artifact builders, and failure envelope.
- [ ] 5.6 REFACTOR — Remove duplicated Taiwan-specific logic from Decision/Backtest services; verify no production strategy, Strategy lookback contract, active assignment, intraday acquisition, cache, fallback, benchmark, workflow activation, or execution state was introduced.
- [ ] 5.7 GREEN — Update README/project metadata to describe provider-neutral Taiwan EOD support, `TWSE`/`TPEX`, reported/unadjusted price basis, provider-breadth vs formal-continuity separation, engine-supported calendar coverage/`CALENDAR_UNAVAILABLE`, representative official regression evidence, conditional sparse overrides, and deferred Strategy lookback, corporate-action methodology, fallback/cache, production strategy/workflow activation, intraday, prediction, and execution work.
- [ ] 5.8 VERIFY — Run `uv run pytest`, `uv run ruff check .`, `uv run ruff format --check .`, and `uv run mypy src tests` from a clean environment.
- [ ] 5.9 VERIFY — Run `openspec validate --all --strict --json --no-interactive`; confirm proposal → specs → design → tasks forward traceability and tasks → design → specs → proposal reverse traceability before declaring the change implementation-ready.
