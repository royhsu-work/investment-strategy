# Tasks — integrate-taiwan-eod-market-data

## 1. Venue-aware market-data identity and pre-load configuration failure

- [x] 1.1 RED — Add outside-in Decision and analytical Backtest behavior tests for configured `TWSE` and `TPEX` instruments proving market-data loading receives a provider-neutral identity containing canonical symbol + listing venue while StrategyContext still exposes only the canonical symbol. Trace: proposal instrument identity; `strategy-engine` listing-venue requirements; design Decisions 1–2.
- [x] 1.2 RED — Add configuration behavior tests for missing listing venue and unsupported/provider-specific venue values, expecting `CONFIGURATION_FAILED / LISTING_VENUE_NOT_CONFIGURED` or `CONFIGURATION_FAILED / UNSUPPORTED_LISTING_VENUE`; prove market-data loading and Strategy evaluation do not run. Run first and verify failures are caused by missing venue behavior.
- [x] 1.3 RED — Add YAML registry tests proving `TWSE` and `TPEX` load as provider-neutral venue identity and remain independent of active strategy assignment, including an instrument with valid venue and no active assignment.
- [x] 1.4 GREEN — Implement immutable market-data instrument identity, extend instrument/YAML configuration with optional listing venue, validate supported venues during configuration resolution, carry the identity in resolved application configuration, and update MarketDataGateway/data-preparation boundaries to consume it.
- [x] 1.5 REFACTOR — Keep listing venue out of StrategyContext and Strategy-specific inputs; keep YAML/provider ticker syntax out of domain behavior; concrete adapters must not query InstrumentRegistry directly.
- [x] 1.6 VERIFY — Run slice tests, then `uv run pytest`, `uv run ruff check .`, `uv run ruff format --check .`, and `uv run mypy src tests`.

## 2. Source-native completed-daily OHLCV through a replaceable provider adapter

- [x] 2.1 RED — Add adapter tests for one TWSE and one TPEx instrument proving provider-specific mapping occurs only inside the concrete adapter (`00733 + TWSE -> 00733.TW`, `00679B + TPEX -> 00679B.TWO`) and the boundary returns plain candidate records rather than provider SDK/DataFrame types. Trace: `taiwan-eod-market-data` provider isolation; design Decision 3.
- [x] 2.2 RED — Add an injected/fake history-loader test that captures invocation arguments and proves native daily history is requested with adapter-controlled automatic adjustment, back-adjustment, and repair disabled; prove `Adj Close`, actions, and repair metadata are not substituted into formal OHLCV.
- [x] 2.3 RED — Add candidate-conversion tests proving timestamp/open/high/low/close/volume pass through without interpolation or synthetic filling; missing/invalid candidate fields remain visible to existing normalization/structural validation.
- [x] 2.4 RED — Add acquisition tests proving adapter exception or empty response reaches `DATA_FAILED / DATA_UNAVAILABLE`, while acquired invalid observations retain structural failure codes and never become `NEUTRAL`.
- [x] 2.5 GREEN — Add the yfinance EOD infrastructure adapter and dependency with explicit behavior equivalent to `period=max`, `interval=1d`, `prepost=false`, `auto_adjust=false`, `back_adjust=false`, `repair=false`, `actions=false`, `rounding=false`, and exception-raising behavior; convert output to plain candidate mappings.
- [x] 2.6 REFACTOR — Isolate all yfinance/pandas imports, ticker suffix rules, response-column handling, and provider exceptions under infrastructure; document/test that `period=max` is acquisition breadth only and does not define analytical or continuity scope.
- [x] 2.7 VERIFY — Run adapter/price-basis/acquisition tests plus full pytest/ruff/mypy checks; no default test may require live external network access.

## 3. Formal-range continuity and freshness remain independent from provider breadth

- [x] 3.1 RED — Add validation behavior tests where provider candidates contain an older gap before an explicitly selected formal historical range, while the selected formal range is complete through `resolved_as_of`; prove the older unrelated gap does not produce `DATA_GAP`. Trace: `market-data-validation` provider-breadth requirement; design Decision 4.
- [x] 3.2 RED — Add a paired test where a trading-day observation is missing inside an explicitly selected formal historical range; prove continuity still fails with `DATA_FAILED / DATA_GAP`.
- [x] 3.3 RED — Add freshness tests proving the latest required completed trading day must equal `resolved_as_of` regardless of extra older history; missing latest data remains `DATA_FAILED / STALE_DATA`.
- [x] 3.4 RED — Add an equivalence test proving two provider responses with identical selected formal history but different extra older observations produce equivalent data eligibility/Strategy input.
- [x] 3.5 GREEN — Refactor combined continuity/freshness behavior so freshness remains tied to `resolved_as_of`, while continuity receives an explicit formal range (or equivalent selected history) and no longer infers its start from the provider's first acquired bar.
- [x] 3.6 REFACTOR — Do not introduce a Strategy-specific lookback/history-window contract in this change; if no analytical caller has selected a formal continuity range, the EOD layer must not invent one from provider response breadth.
- [x] 3.7 VERIFY — Run continuity/freshness/regression tests plus full pytest/ruff/mypy checks.

## 4. Taiwan regular-securities TradingCalendar with engine-supported coverage

- [x] 4.1 RED — Add timezone-aware Taiwan-calendar tests proving `market_date()` uses `Asia/Taipei` and is independent of runner/local timezone. Trace: `taiwan-eod-market-data` timezone/calendar requirements; design Decisions 6–10.
- [x] 4.2 RED — Add repository-maintained deterministic regression fixtures using the following lead-approved official evidence, with fixture metadata containing `expected_session`, source URL(s), retrieval date `2026-08-11`, and a short paraphrased evidence note (do not copy full source pages):
  - `2018-02-21` → `SESSION`: TPEx 2018 Holiday Schedule states that the regular listed-stock market reopens on February 21 after Lunar New Year. Source: `https://www.tpex.org.tw/storage/en-us/web/bulletin/trading_date/trading_date_107.htm`.
  - `2018-02-28` → `NON_SESSION`: the same TPEx 2018 Holiday Schedule identifies February 28 as Peace Memorial Day. Source: `https://www.tpex.org.tw/storage/en-us/web/bulletin/trading_date/trading_date_107.htm`.
  - `2018-03-31` → `SESSION`: the same TPEx 2018 Holiday Schedule explicitly lists March 31 as an additional Saturday trading day; TWSE official March 2018 daily market-price data independently shows regular market-time closing data on March 31, confirming the common regular-securities market was open. Sources: `https://www.tpex.org.tw/storage/en-us/web/bulletin/trading_date/trading_date_107.htm` and `https://www.twse.com.tw/exchangeReport/STOCK_DAY_AVG?date=20180301&response=html&stockNo=3607` (an equivalent TWSE-listed security report containing `107/03/31` may be used if this URL is unavailable).
  - `2024-07-24` → `NON_SESSION`: use the TWSE natural-disaster closure rule together with the Taipei City Government announcement that all offices and schools were closed on July 24 because of Typhoon Gaemi. The TWSE rule states that when Taipei City announces a full-day government-office closure before the trading day, the centralized market closes for the entire day. Sources: `https://www.twse.com.tw/zh/clearing/suspended.html` and `https://www.gov.taipei/News_Content.aspx?n=2044902FC839D045&s=D3217C15B96246EA&sms=72544237BBE4C5F6`. Optional corroboration: `https://www.twse.com.tw/zh/ETFortune/announcement?company=A00005&date=20240812&fund=0056&seq=2&type=all`, which explicitly refers to July 24 and July 25 as typhoon holidays.
  These four cases are regression evidence only; they do not create a duplicate full historical calendar or new public contract.
- [x] 4.3 RED — Add previous/latest-completed-session tests before and after the conservative `13:33 Asia/Taipei` completion boundary.
- [x] 4.4 RED — Add calendar-coverage tests proving a required date/range that the configured pinned XTAI engine cannot answer because it is outside its actual supported bounds or otherwise unavailable fails with `DATA_FAILED / CALENDAR_UNAVAILABLE` before `DATA_GAP` or `STALE_DATA`; do not invent separate hard-coded earliest/latest project dates.
- [x] 4.5 RED — Add override-precedence contract tests using deterministic injected/fixture engine behavior: when engine truth and an explicit repository override disagree for the same date, the override wins; when no override exists, the engine answer is used. A real production override entry is not required by this task unless Task 4.2 or other official evidence identifies an actual XTAI discrepancy.
- [x] 4.6 GREEN — Implement a replaceable Taiwan regular-securities TradingCalendar adapter using a pinned `exchange_calendars` XTAI engine, `Asia/Taipei`, the conservative current-day completion rule, and engine-supported coverage semantics. The adapter may inspect/expose the engine's actual session bounds internally; dates the engine cannot establish map to `CALENDAR_UNAVAILABLE`.
- [x] 4.7 GREEN — Implement sparse repository override support and precedence. Compare the pinned XTAI result for the approved `2018-03-31` session fixture: if XTAI reports non-session, that is a real officially evidenced discrepancy and a sparse production override for that date is required. Add any other production override only when separate official evidence proves another TWSE/TPEx regular-market discrepancy; never fabricate an override merely to satisfy the task.
- [x] 4.8 REFACTOR — Keep external calendar-library types/version-specific APIs inside infrastructure; do not maintain a duplicate full calendar, do not require per-date official certification across the engine range, and do not add automatic future/exceptional-session discovery, International Bond Market, or Emerging Stock Board semantics.
- [x] 4.9 VERIFY — Validate the pinned XTAI plus repository overrides against all four approved Task 4.2 fixtures, then run Taiwan-calendar/session/coverage/override tests plus full pytest/ruff/mypy checks. Record whether each fixture matched the engine directly or required a repository override.

## 5. Decision/Backtest integration, baseline regressions, and OpenSpec completion

- [x] 5.1 RED — Add an outside-in Decision composition test using configured Taiwan venue, concrete provider adapter with injected deterministic EOD data, Taiwan TradingCalendar, fixed Clock, and test-only Strategy; expect successful evaluation from completed EOD facts with no intraday/realtime value entering Strategy evaluation. Trace: proposal existing-service consumption; design Decisions 1, 3, 9–12.
- [x] 5.2 RED — Add an analytical Backtest composition test over a historical Taiwan range proving the same provider/calendar path preserves chronological replay, WARMUP/no-look-ahead behavior, and no provider-specific fields enter StrategyContext.
- [x] 5.3 RED — Add integration cases proving incomplete current session is not inserted as a formal daily bar, reliable missing latest session remains `STALE_DATA`, and engine-unavailable calendar status is not rewritten as `DATA_GAP`/`STALE_DATA`.
- [x] 5.4 BASELINE GREEN — Add/retain regression assertions that Decision and analytical Backtest public request/artifact schemas and exact disclaimer remain unchanged. These assertions must be GREEN before Taiwan composition implementation and remain GREEN afterward; they are not used to manufacture a RED state.
- [x] 5.5 GREEN — Update Decision/Backtest data-loading orchestration only as needed to pass provider-neutral market-data identity, separate freshness from explicit-range continuity, and consume the Taiwan calendar; reuse existing normalization, Strategy evaluator, artifact builders, and failure envelope.
- [x] 5.6 REFACTOR — Remove duplicated Taiwan-specific logic from Decision/Backtest services; verify no production strategy, Strategy lookback contract, active assignment, intraday acquisition, cache, fallback, benchmark, workflow activation, or execution state was introduced.
- [x] 5.7 GREEN — Update README/project metadata to describe provider-neutral Taiwan EOD support, `TWSE`/`TPEX`, source-native OHLCV price basis, provider-breadth vs formal-continuity separation, engine-supported calendar coverage/`CALENDAR_UNAVAILABLE`, representative official regression evidence, conditional sparse overrides, and deferred Strategy lookback, corporate-action methodology, fallback/cache, production strategy/workflow activation, intraday, prediction, and execution work.
- [x] 5.8 VERIFY — Run `uv run pytest`, `uv run ruff check .`, `uv run ruff format --check .`, and `uv run mypy src tests` from a clean environment.
- [x] 5.9 VERIFY — Run `openspec validate --all --strict --json --no-interactive`; confirm proposal → specs → design → tasks forward traceability and tasks → design → specs → proposal reverse traceability before declaring the change implementation-ready.

## 6. PR #10 review corrections

- [ ] 6.1 RED — Replace any regression/test assertion that equates `auto_adjust=false` or absence of `Adj Close` with exchange-raw/unadjusted historical prices. Add a source-native price-basis test proving formal OHLCV comes directly from the provider native `Open`/`High`/`Low`/`Close`/`Volume` fields, `Adj Close` is ignored, and the adapter applies no additional OHLC transformation. Trace: proposal source-native price basis; `market-data-validation` source-native requirement; design Decision 3.
- [ ] 6.2 RED — Add normalization regression tests for non-finite numeric provider candidates (`NaN`, `+Infinity`, `-Infinity`) proving acquired invalid observations fail through the canonical `DATA_FAILED` structural validation envelope rather than escaping as raw `decimal.InvalidOperation` or another implementation exception. Trace: existing canonical market-data structural-validation contract and design Decision 5.
- [ ] 6.3 RED — Add a Taiwan calendar regression proving `is_session_complete()` returns false for historical non-sessions, using the approved `2024-07-24` exceptional-closure fixture. Trace: `taiwan-eod-market-data` completed-session and exceptional-closure requirements; design Decisions 6, 8–9.
- [ ] 6.4 GREEN — Correct normalization to reject non-finite numeric values with a canonical validation failure; correct historical session-completion ordering so non-trading dates are never considered completed sessions; update adapter comments/naming and README wording so they say source-native/provider-native rather than exchange-raw/reported-unadjusted where applicable. Do not add corporate-action reconstruction or analytical adjustment behavior.
- [ ] 6.5 VERIFY — Re-run the focused provider/normalization/calendar tests, then `uv run pytest`, `uv run ruff check .`, `uv run ruff format --check .`, `uv run mypy src tests`, `openspec list`, and `openspec validate --all --strict --json --no-interactive`; re-check bidirectional proposal ↔ specs ↔ design ↔ tasks traceability before requesting PR #10 re-review.
