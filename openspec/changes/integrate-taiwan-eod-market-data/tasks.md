# Tasks — integrate-taiwan-eod-market-data

## 1. Venue-aware market-data identity and pre-load configuration failure

- [ ] 1.1 RED — Add outside-in Decision and analytical Backtest behavior tests for configured `TWSE` and `TPEX` instruments proving market-data loading receives a provider-neutral identity containing canonical symbol + listing venue while the StrategyContext still exposes only the canonical symbol. Trace: proposal `What Changes` instrument identity; `strategy-engine` listing-venue requirements; design Decisions 1–2.
- [ ] 1.2 RED — Add configuration behavior tests for a missing listing venue and an unsupported/provider-specific venue value (for example a ticker suffix), expecting `CONFIGURATION_FAILED / LISTING_VENUE_NOT_CONFIGURED` or `CONFIGURATION_FAILED / UNSUPPORTED_LISTING_VENUE` respectively; use a spy gateway and prove market-data loading and Strategy evaluation do not run. Run these tests before production changes and verify the RED failures are caused by the missing venue behavior rather than fixture/import/setup errors.
- [ ] 1.3 RED — Add YAML registry tests proving human-readable `TWSE` and `TPEX` values load as provider-neutral venue identity and remain independent of active strategy assignment; include an instrument with valid venue and no active assignment.
- [ ] 1.4 GREEN — Implement the minimum immutable market-data instrument identity, extend instrument/YAML configuration with optional listing venue, validate the supported venue set during configuration resolution, carry the validated identity in resolved application configuration, and change the MarketDataGateway/data-preparation call boundary to consume that identity rather than a provider-formatted string.
- [ ] 1.5 REFACTOR — Keep listing venue out of StrategyContext/Strategy-specific inputs, keep YAML/provider ticker syntax out of domain behavior, and remove any need for a concrete market-data adapter to query InstrumentRegistry directly.
- [ ] 1.6 VERIFY — Run the venue/configuration slice tests, then `uv run pytest`, `uv run ruff check .`, `uv run ruff format --check .`, and `uv run mypy src tests`; complete this slice only when all are green.

## 2. Reported completed-daily OHLCV through a replaceable provider adapter

- [ ] 2.1 RED — Add adapter tests for a TWSE instrument and a TPEx instrument proving provider-specific identifier mapping occurs only inside the concrete adapter (for the first implementation, `00733 + TWSE -> 00733.TW` and `00679B + TPEX -> 00679B.TWO`) and the returned boundary value is plain candidate records rather than provider SDK/DataFrame types. Trace: `taiwan-eod-market-data` canonical-identity/provider-isolation requirements; design Decision 3.
- [ ] 2.2 RED — Add an injected/fake history-loader test that captures provider invocation arguments and proves the adapter explicitly requests daily reported history with automatic adjustment, back-adjustment, and repair disabled; also prove provider `Adj Close`, dividends/splits/actions, and repair metadata are not substituted into formal OHLCV. Run and verify the intended RED failure before implementation.
- [ ] 2.3 RED — Add candidate-conversion tests proving timestamp/open/high/low/close/volume are passed through without interpolation or synthetic filling; missing/invalid candidate fields remain visible to the existing normalization/validation pipeline rather than being fabricated by the adapter.
- [ ] 2.4 RED — Add acquisition integration tests proving an adapter exception or empty provider response reaches existing `DATA_FAILED / DATA_UNAVAILABLE` behavior, while acquired invalid observations retain existing structural failure codes; verify the provider implementation cannot convert these failures into `NEUTRAL`.
- [ ] 2.5 GREEN — Add the concrete yfinance EOD infrastructure adapter and dependency with explicit `period=max`, `interval=1d`, `prepost=false`, `auto_adjust=false`, `back_adjust=false`, `repair=false`, `actions=false`, `rounding=false`, and exception-raising behavior; convert provider output into plain raw candidate mappings consumed by the existing preparation pipeline.
- [ ] 2.6 REFACTOR — Isolate all yfinance/pandas imports, ticker suffix rules, response-column handling, and provider exceptions under infrastructure; keep `MarketDataGateway`, Decision, Backtest, normalization, validation, and Strategy contracts free of provider SDK types.
- [ ] 2.7 VERIFY — Run provider-adapter/price-basis/acquisition tests plus the full pytest/ruff/mypy checks; no default test may require live external network access.

## 3. Taiwan regular-securities TradingCalendar behavior

- [ ] 3.1 RED — Add Taiwan-calendar behavior tests using timezone-aware fixed instants proving `market_date()` is based on `Asia/Taipei` and is unchanged by the runner/local timezone. Trace: `taiwan-eod-market-data` timezone/calendar requirements; design Decisions 5–7.
- [ ] 3.2 RED — Add deterministic session-regression tests covering a normal trading weekday, an official weekday holiday, a historical additional Saturday trading session, and an exceptional full-market closure; include representative official schedule fixtures for both TWSE and TPEx regular listed-securities/ETF markets rather than a weekday-only calendar.
- [ ] 3.3 RED — Add previous/latest-completed-session tests for a current trading date before completion and after completion, including the conservative `13:33 Asia/Taipei` completion boundary; prove an in-progress current session resolves to the previous completed session and a completed current session becomes calendar-eligible.
- [ ] 3.4 RED — Add a stale-provider integration test where the calendar declares current T completed but acquired history ends at T-1; prove `resolved_as_of` remains T and existing `DATA_FAILED / STALE_DATA` behavior applies instead of calendar fallback. Run all calendar RED tests and verify failures are caused by missing concrete Taiwan calendar behavior.
- [ ] 3.5 GREEN — Implement a replaceable Taiwan regular-securities TradingCalendar adapter behind the existing protocol, using `exchange_calendars` XTAI as the initial session engine and `Asia/Taipei` plus the conservative current-day completion rule; map both `TWSE` and `TPEX` regular listed-securities/ETF evaluation to this behavior for this change.
- [ ] 3.6 GREEN — Where required by the regression fixtures, add only minimal explicit session exceptions/overrides needed to preserve known TWSE/TPEx regular-market truth; do not add International Bond Market, Emerging Stock Board, intraday, or unrelated venue semantics.
- [ ] 3.7 REFACTOR — Keep external calendar-library types and version-specific APIs inside infrastructure, keep provider-returned dates from defining exchange sessions, and preserve the existing TradingCalendar application contract.
- [ ] 3.8 VERIFY — Run Taiwan-calendar/session/freshness tests plus full pytest/ruff/mypy checks.

## 4. Decision and analytical Backtest consume Taiwan EOD data without public-contract changes

- [ ] 4.1 RED — Add an outside-in Decision integration test using a configured Taiwan venue, the concrete provider adapter with injected deterministic EOD provider data, the Taiwan TradingCalendar, a fixed Clock, and a test-only Strategy; expect successful evaluation from completed daily OHLCV and prove no intraday/realtime value enters Strategy evaluation. Trace: proposal existing-service consumption; `taiwan-eod-market-data` completed-EOD requirements; design Decisions 1, 7, 10.
- [ ] 4.2 RED — Add an analytical Backtest integration test over a historical Taiwan range proving the same EOD adapter/calendar path preserves chronological replay, pre-roll/minimum-history behavior, and no-look-ahead without provider-specific fields entering StrategyContext.
- [ ] 4.3 RED — Add integration cases proving a missing expected Taiwan session remains `DATA_GAP`, a missing latest completed session remains `STALE_DATA`, and an incomplete current session is never inserted as a formal daily bar.
- [ ] 4.4 RED — Add regression assertions that Decision and analytical Backtest public request/artifact schemas remain unchanged by this change and existing public artifacts still contain exactly `僅為個人研究與策略驗證，不構成任何形式之投資建議。`; run and confirm intended RED failures relate only to the new production data/calendar composition.
- [ ] 4.5 GREEN — Update Decision/Backtest data-loading orchestration only as needed to pass the resolved provider-neutral market-data identity into the gateway and consume the Taiwan calendar; reuse the existing normalization, validation, Strategy evaluator, artifact builders, and failure semantics without adding parallel paths.
- [ ] 4.6 REFACTOR — Remove duplicated Taiwan-specific logic from Decision/Backtest services, keep provider/calendar construction in infrastructure/composition seams, and verify no production strategy, active assignment, intraday acquisition, cache, fallback, or execution state was introduced.
- [ ] 4.7 VERIFY — Run Decision/Backtest Taiwan EOD integration tests, existing Decision/Backtest regression tests, and full pytest/ruff/mypy checks.

## 5. Repository contract, documentation, and OpenSpec completion

- [ ] 5.1 RED — Add or update repository-contract tests that fail while README/project metadata still claim concrete market-data provider and Taiwan calendar are entirely deferred; verify the RED failure targets documentation/repository-contract drift rather than runtime behavior. Trace: proposal Impact/Deferred Work; design Decisions 3, 5, 10.
- [ ] 5.2 GREEN — Update README documentation to describe the provider-neutral Taiwan EOD capability, supported `TWSE`/`TPEX` venue configuration, EOD-only/current-state semantics, concrete adapter/calendar as replaceable infrastructure details, reported/unadjusted formal price basis, and explicitly deferred corporate-action methodology, fallback/cache, production strategy, workflow activation, intraday, prediction, and execution work.
- [ ] 5.3 GREEN — Update dependency/project metadata required by the concrete adapters without exposing provider packages as domain/application dependencies; keep `config/instruments.yaml` free of a real active production strategy assignment in this change.
- [ ] 5.4 REFACTOR — Review the implementation against proposal scope and remove any opportunistic realtime, prediction, corporate-action adjustment, fallback, cache/database, workflow-activation, strategy-algorithm, benchmark, or execution behavior.
- [ ] 5.5 VERIFY — Run `uv run pytest`, `uv run ruff check .`, `uv run ruff format --check .`, and `uv run mypy src tests` from a clean checkout/environment.
- [ ] 5.6 VERIFY — Run `openspec validate --all --strict --json --no-interactive` and resolve every issue; confirm proposal → specs → design → tasks forward traceability and tasks → design → specs → proposal reverse traceability before declaring the change ready for implementation.
