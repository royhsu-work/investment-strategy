# Tasks: Align Scheduled OpenSpec semantics

## Slice 1. Scheduled actions load one bounded semantic adapter

- [x] 1.1 **RED:** Add governance tests proving applicable Scheduled OpenSpec actions resolve one shared `spec-driven` semantic-adapter reference, fail when that configured semantic contract is unavailable/unsupported, and preserve existing runtime routing rather than treating adapter loading as a second workflow authority. Verify each RED failure is caused by missing adapter behavior. Trace: proposal `What Changes` → spec `Scheduled roles preserve material OpenSpec semantics when CLI instructions are unavailable` → design Decisions 1/2.
- [x] 1.2 **GREEN:** Add the shared progressive-disclosure adapter with the already-decided contract and immutable provenance `Fission-AI/OpenSpec@2826b8889e5223a9a8095d4428b60b56597e1020` `schemas/spec-driven/schema.yaml`, including the represented proposal → specs / proposal → design / specs+design → tasks / tasks → Apply dependency-readiness graph and applicable `openspec/config.yaml` context/rules. Executor MUST transcribe this decided contract and MUST NOT select which OpenSpec semantics belong in it. Trace: same requirement → design Decisions 2.1/2.2/2.6.
- [x] 1.3 **GREEN:** Update `openspec-change`, `openspec-review`, and `implementation` skills to load the shared reference only for applicable OpenSpec actions; add only the minimum role/shared-governance reference needed to make ownership/consumption reconstructable without duplicating the adapter contract into `AGENTS.md` or role files. Trace: same requirement → design Decisions 1/3/4/5.
- [x] 1.4 **REFACTOR / VERIFY:** Remove any duplicated action-local semantic copy made obsolete by the shared adapter while preserving existing role authority. Run Slice 1 tests, full regression suite, type checks, lint, and format checks; complete the slice only when all are green.

## Slice 2. Propose and independent review reject incomplete delta/canonicalization semantics

- [x] 2.1 **RED:** Add regression coverage for the #29 failure class: a NEW capability lacking exactly one non-empty Purpose must not reach successful `review-openspec` PASS even when strict OpenSpec validation accepts the artifact set. Verify the RED failure is the missing canonicalization-ready behavior. Trace: proposal `Why` → spec `OpenSpec authoring and independent review prevent knowable canonicalization omissions` → design Decisions 2.4/3/4.
- [x] 2.2 **RED:** Add regression coverage that a MODIFIED requirement must use the exact existing canonical header and carry the complete future requirement block with every still-applicable scenario/content; partial MODIFIED content must be rejected before implementation. Trace: spec adapter delta-authoring contract → design Decision 2.3.
- [x] 2.3 **RED:** Add regression coverage for ADDED/REMOVED/RENAMED semantics: ADDED cannot collide with canonical identifiers; REMOVED targets an existing requirement and carries required rationale/migration treatment; RENAMED uses exact FROM/TO; rename plus behavior change additionally requires a complete MODIFIED block under the new header. Trace: same requirement → design Decision 2.3.
- [x] 2.4 **GREEN:** Implement Lead authoring guidance and independent Reviewer verification through the shared adapter so NEW-capability Purpose/canonical target matching plus complete ADDED/MODIFIED/REMOVED/RENAMED semantics are checked before implementation handoff. Preserve surviving MODIFIED scenarios/content and fail closed on unsupported or ambiguous semantic inputs. Trace: spec `OpenSpec authoring and independent review prevent knowable canonicalization omissions` → design Decisions 2.3/2.4/3/4.
- [x] 2.5 **REFACTOR / VERIFY:** Consolidate only duplicated delta/canonicalization checks while keeping reverse-first plus forward traceability and exact-head validation as independent existing gates. Run Slice 2 tests, full regression suite, type checks, lint, and format checks; complete the slice only when all are green.

## Slice 3. Executor receives complete approved Apply context and fails closed otherwise

- [x] 3.1 **RED:** Add coverage showing Executor fails closed/returns to Lead when any required approved proposal, applicable delta spec, design, tasks, needed canonical spec, or applicable config context/rule is missing, contradictory, materially ambiguous, or inconsistent with the represented schema/baseline. Verify RED is caused by the missing/incorrect Apply-context behavior. Trace: proposal Apply correction → spec `Executor consumes complete approved OpenSpec apply context` → design Decisions 2.5/5.
- [x] 3.2 **RED:** Add coverage proving Executor does not choose which upstream/config semantics count, resolve material spec/design ambiguity, or reinterpret task meaning when adapter/context is incomplete. Trace: same requirement → design Decisions 2.5/5.
- [x] 3.3 **GREEN:** Implement Executor consumption of the closed approved Apply context through the shared adapter: proposal, applicable delta specs, design, tasks, canonical specs needed to interpret modified behavior, and applicable `openspec/config.yaml` context/rules. Preserve the Lead specification-authority boundary and return unresolved material meaning to Lead. Trace: same requirement → design Decision 5.
- [x] 3.4 **REFACTOR / VERIFY:** Simplify duplicated context-loading paths without broadening Executor authority. Run Slice 3 tests, full regression suite, type checks, lint, and format checks; complete the slice only when all are green.

## Slice 4. Semantic provenance makes unsupported baseline drift observable and fail closed

- [x] 4.1 **RED:** Add regression coverage that a material configured-schema or adapter-baseline mismatch fails closed until deliberate adapter reconciliation, rather than silently using model memory, mutable upstream `main`, or unrelated executable behavior. Verify RED is caused by missing provenance/reassessment behavior. Trace: proposal durable sources → spec provenance contract → design Decisions 2.6/6.
- [x] 4.2 **GREEN:** Record and expose immutable adapter provenance for `Fission-AI/OpenSpec@2826b8889e5223a9a8095d4428b60b56597e1020` `schemas/spec-driven/schema.yaml` plus the observed executable baseline `@fission-ai/openspec@1.3.1`, and implement fail-closed reassessment when the configured schema/material contract no longer matches that represented baseline. Do not change executable package pins or Archive compatibility guards in this Change. Trace: same requirement → design Decisions 2.6/6.
- [x] 4.3 **REFACTOR / VERIFY:** Keep provenance data at the narrowest reusable ownership surface and remove redundant mutable-baseline references. Run Slice 4 tests, full regression suite, type checks, lint, and format checks; complete the slice only when all are green.

## Final change verification and review readiness

- [x] 5.1 Run the complete targeted governance suite, full regression tests, lint/format/type checks, and strict OpenSpec validation; resolve failures within approved scope.
- [x] 5.2 Confirm executable OpenSpec package pins and Archive compatibility guards are unchanged and #63 remains the only executable-version upgrade track.
- [x] 5.3 Confirm required trace declarations remain mechanically and semantically bidirectional across proposal → specs → design → tasks and tasks → design → specs → proposal before independent OpenSpec review.
