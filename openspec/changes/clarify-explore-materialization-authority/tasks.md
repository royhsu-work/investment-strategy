# Tasks

## 1. Source-decision-owned bounded Explore materialization

- [ ] RED: add focused behavioral tests proving an explicit Human interaction-layer request for one bounded Formal Explore requires the complete open Issue + `Change: unset` + `action:explore-change` tuple, and that Issue-only materialization fails its postcondition.
- [ ] RED: add regressions proving physical connector writer identity neither removes qualified materialization authority nor becomes sufficient authority for a later Human-reserved decision; autonomous Agent recommendation alone remains insufficient.
- [ ] RED: verify a coherently materialized Explore remains subject to existing formal-first/WIP=1/finish-first ordering and later dispatch is origin-neutral.
- [ ] GREEN: make the minimum producer/materialization-boundary implementation and shared-governance wording correction needed to satisfy the approved delta without introducing new workflow state, authority registries, tokens, Actions, or routing dimensions.
- [ ] REFACTOR: remove or consolidate actor-centric duplicate guidance where the canonical source-decision rule now owns the consequence; keep Role, Skill, Action model, topology, and OpenSpec config unchanged unless a concrete failing regression proves otherwise.
- [ ] VERIFY: run focused materialization/dispatch tests, the full regression suite, type checks, lint checks, and strict OpenSpec validation; confirm no #229/#233 behavior or generic Human Explore admission semantics were imported.

Refs #234
